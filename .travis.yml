sudo: required
dist: bionic

before_install:
  - sudo apt-get update -yq
  - sudo apt-get install -y context wget unzip make asymptote fonts-dejavu

script:
  - make get-fonts
  - make
  - make pages
